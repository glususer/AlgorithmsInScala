package Leetcode

object Sort {
  def main(args: Array[String]): Unit = {
   /* val arr = args.head.split(" ").map(_.toInt)
    quickSort(arr)

    def quickSort(arr: Array[Int]): Array[Int] = {
      def helper(arr: Array[Int]): Array[Int] = {
        if (arr.isEmpty) return Array.empty
        val pi = arr.last
        val (left, right) = arr.partition(ele => ele <= pi)
        println(left, right)
        helper(left) ++ helper(right)
      }

      helper(arr)
    }*/
  }

 // Sort.main(Array("1 2 3 4 5"))
}




